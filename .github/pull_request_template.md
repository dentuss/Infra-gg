## Summary

<!-- What does this PR do and why? -->

## Changes

-

## Checklist

<!-- Title must be a Conventional Commit, e.g. "feat(board): add layer list".
     Base branch is dev, unless this is a hotfix or a dev → master release. -->

- [ ] TypeScript passes (`npm run typecheck`)
- [ ] ESLint passes (`npm run lint`)
- [ ] Formatting passes (`npm run format:check`)
- [ ] Unit tests pass (`npm test`)
- [ ] Production build succeeds (`npm run build`)
- [ ] New user-facing strings added to **both** `messages/en.json` and
      `messages/ru.json`
- [ ] Migrations applied to `infragg-dev` and verified there
- [ ] TASKS.md updated if a roadmap task was completed
- [ ] No secrets committed
